
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 🔓 FULLY OPEN RULES FOR PROTOTYPE DEVELOPMENT
    // This ensures no permission errors occur during testing.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
